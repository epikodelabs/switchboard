export {
  applyBaseHref,
  getRouterLocation as getNavigationLocation,
  isPathInsideBase,
  normalizeBaseHref,
  normalizePath,
  resolveRouterUrl as resolveNavigationUrl,
  routerHref as navigationHref,
  stripBaseHref,
  type RouterUrlMode as NavigationUrlMode,
} from './router-url';

